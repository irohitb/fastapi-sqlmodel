## Pipenv

```
pyenv -> nvm
pyenv-virtualenv
pyenv install 3.10.x
pyenv virutalenv 3.9.x sproject
pyenv activate sproject
pip install pipenv
pipenv install
pipenv shell ->
pipenv run python asgi.py
```
